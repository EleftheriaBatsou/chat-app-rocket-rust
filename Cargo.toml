[package]
name = "chat-app-rocket-rust"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rocket = { features = ["json"] }

[dev-dependencies]
rand = "0.8"
